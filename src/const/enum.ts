/** 签到日历状态 */
export enum DailyCheckIn {
  Able = 'check-in',
  Wating = 'wating',
  Unable = 'unable',
  Done = 'done',
}

/** My-tickes */

export enum  TicketType {
  APL,
  Peso,
  Ticket,
}

export enum TicketStatus {
  Valid,
  Used,
  Expired,
}
